# Checkpoint playbook

At a logical boundary, record:

- Objective and current phase.
- Decisions and assumptions.
- Changed files.
- Checks run and their results.
- Open risks and the next action.

Use `node scripts/codex/checkpoint.mjs --out .codex/checkpoint.md` when a file checkpoint is useful.
