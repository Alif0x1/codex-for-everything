# Skill map

The runtime skills are kept under `plugins/codex-for-everything/skills/` so Codex can cache and load them reliably. This root directory is reserved for future cross-harness skill indexes; do not duplicate runtime `SKILL.md` files here.
