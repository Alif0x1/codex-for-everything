# Operations rule

- Separate local, test, staging, and production hosts and credentials.
- Verify migrations against the configured database before recovery actions.
- Make external side effects observable and idempotent where possible.
- Report provider, webhook, email, deployment, and browser checks separately.
- Prefer reversible operations and explicit approval for destructive changes.
