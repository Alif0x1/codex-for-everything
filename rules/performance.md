# Performance rule

- Measure before optimizing when the change is not obvious.
- Avoid unnecessary client JavaScript, repeated fetches, layout shifts, and unbounded lists.
- Keep continuous UI values out of React state.
- Respect caching, invalidation, pagination, and provider rate limits.
- Treat performance budgets as product constraints.
