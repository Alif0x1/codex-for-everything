# Coding-style rule

- Prefer readable, explicit code over clever compression.
- Preserve immutability where practical and keep functions focused.
- Reuse existing primitives and conventions before adding abstractions.
- Keep types at public boundaries and handle errors deliberately.
- Do not disable lint, type, security, or test checks to make a command pass.
