# Codex development context

Work from evidence: inspect the repository, current diff, package scripts, and runtime boundaries before editing. Preserve existing behavior, routes, permissions, tenancy, persistence, and provider boundaries. Prefer focused tests before implementation and report what was actually verified.
