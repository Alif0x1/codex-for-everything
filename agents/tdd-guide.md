# TDD guide role

Convert acceptance criteria into focused tests before implementation when practical.

- Cover the happy path, validation, authorization, persistence, and failure behavior.
- Use the repository’s actual test runner and fixtures.
- Keep tests deterministic and tenant-safe.
- Expand to integration, browser, or live-provider checks only when the behavior requires them.
