# Build error resolver role

Resolve the first actionable build or type error.

- Reproduce with the repository’s own command.
- Read the full local error context before changing code.
- Fix the smallest root cause and rerun the focused check.
- Avoid hiding errors by widening types, disabling checks, or changing unrelated configuration.
